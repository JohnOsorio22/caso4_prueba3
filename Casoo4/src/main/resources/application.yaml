spring:
  application:
    name: casoo4-rastreo

  profiles:
    active: prod

  datasource:
    url: ${DB_URL:jdbc:oracle:thin:@(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.sa-santiago-1.oraclecloud.com))(connect_data=(service_name=g314a68833b0d91_fullstackhacks_medium.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))}
    username: ${DB_USER:ADMIN}
    password: ${DB_PASS:FastTrack2024#!}
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: ${SERVER_PORT:28000}


flota:
  url: ${FLOTA_URL:http://localhost:16000}


springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
